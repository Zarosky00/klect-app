import { EntityPageSkeleton } from '@/components/surf/EntityPageSkeleton';

/** The closeup's own shape while `get_closeup` is in flight. */
export default function CloseupLoading() {
  return <EntityPageSkeleton />;
}
