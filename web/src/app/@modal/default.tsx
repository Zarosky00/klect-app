/**
 * The modal slot renders nothing unless an intercepting route matched.
 * Next requires this file so a hard navigation has something to fall back to.
 */
export default function ModalSlotDefault() {
  return null;
}
